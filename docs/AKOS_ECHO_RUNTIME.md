# AKOS + ECHO — Max Runtime Capability Protocol

**AKOS** = Apex Knowledge OS (identity · governance · map · handoff).  
**ECHO** = **E**xternalize · **C**ompact · **H**andoff · **O**rchestrate — token/runtime discipline layer on Grok Build.

**Law:** measure or unknown · pure_pointer · legal absolute private · flippers before LLM spawn.

---

## ECHO loop (every non-trivial turn)

| Step | Action | Tooling |
|------|--------|---------|
| **E**xternalize | Large bodies → disk/state; chat gets ptr only | `token-100pct-savings-flipper` · `state/externalized_blobs/` |
| **C**ompact | Keep context lean; compact earlier | `auto_compact_threshold_percent` ≤ 70 (target **65**) |
| **H**andoff | Start: IDENTITY + CURRENT_STATE + map · End: SESSIONS + state ptr | AKOS `GROK_SWARM_BRIDGE.md` · `CURRENT_STATE.md` |
| **O**rchestrate | AZOP waves: explore∥ → synth → worktree implement | AZOP · subagents · concise persona |

---

## Always-on (T0)

| Layer | Setting |
|-------|---------|
| Skills | `token-saver` · `toolbelt` · `swarm-orchestrator` (as needed) |
| MCP | `sequential-thinking` · `ai-humanizer` (final prose) · connectors JIT |
| Agents | AGENTS L0–L5 · `agent.yolo` for goals · subagents concise |
| Hooks | PreToolUse safe-shell (AZOP SONIC) |
| Flippers | token-100% · device-stability · toolbelt-doctor |

---

## Activation sequence (max capability, min burn)

```
1. toolbelt-doctor + token-100pct (measure)
2. Open map: state/ecosystem_map.json + AKOS CURRENT_STATE (ptrs only)
3. Goal → 5W1H externalize
4. MICROWAVE explore (read-only, background, concise)
5. Parent synth pointers only
6. Implement worktree if multi-file
7. Local py_compile / tests before claim done
8. Humanize final user-facing prose only
9. Flush state; suggest compact if heavy
```

---

## Config targets (GlacierEQ)

| Key | Target | Why |
|-----|--------|-----|
| `session.auto_compact_threshold_percent` | **65** | Earlier compact → less thrash |
| `memory.enabled` | true | Cross-session |
| `memory.search.max_results` | 8 | Dense recall |
| `memory.search.min_score` | 0.35–0.40 | Precision |
| `ui.remember_tool_approvals` | true | Less friction |
| `agent.yolo` | true | Goal autonomy |
| `ui.yolo` | false default | Safety (opt-in) |
| MCP sequential + humanizer | enabled | L0b + L0c |

Apply: `python3 ~/GlacierEQ_Swarm/automations/apply_grok_config_perf.py`  
Maximize harness: `python3 ~/GlacierEQ_Swarm/automations/akos-echo-maximize.py`

---

## Connector / MCP health

| Priority | Connectors |
|----------|------------|
| 0 | Local state · AKOS · Swarm (0 burn) |
| 1 | sequential-thinking · github · notion · brainsync |
| 2 | supabase · supermemory · vercel |
| 3 | gmail/outlook · apple-mcp · humanizer on demand |

**Rule:** search connectors + map **before** spawn. On MCP timeout: continue local; mark unknown; don’t re-paste walls.

---

## Skills load (JIT)

| Always | JIT |
|--------|-----|
| token-saver | path-of-highest-power · repo-indexer · design/execute-plan |
| toolbelt | voice-memo · office · imagine · review |

Never paste full SKILL bodies into chat.

---

## Anti-patterns

- Sequential full-repo reads in parent  
- Bulk public / legal leak  
- Claiming 100% savings without ledger measure  
- Re-expanding AKOS clues every session (use ptr)  
- Dual IDE thrash on 8GB host  

---

## Ptrs

| | |
|--|--|
| This protocol | `job-app/docs/AKOS_ECHO_RUNTIME.md` |
| AKOS bridge | `job-app/repos/AKOS/` · GitHub GlacierEQ/AKOS |
| Toolbelt | `job-app/docs/TOOLBELT.md` |
| Maximize last | `state/akos_echo_maximize_last.json` |
| Token ledger | `state/token_100pct_last.json` |

---

*AKOS governs truth · ECHO governs burn · together maximize runtime capability*
