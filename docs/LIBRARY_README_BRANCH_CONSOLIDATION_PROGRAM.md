# Historical Path — Branch Consolidation Doctrine Retired

This path is retained so historical links do not break. The active program is now:

**[`LIBRARY_CAPABILITY_ELEVATION_PROGRAM.md`](LIBRARY_CAPABILITY_ELEVATION_PROGRAM.md)**

The former `DISCOVER → COMPARE → PRESERVE → VERIFY → MERGE_OR_CLOSE → DELETE_REF → RECEIPT` lifecycle is **retired as execution authority**.

It remains relevant only as historical evidence of the July 2026 control model. It may not authorize closing, archiving, deleting, merging away, or demoting repositories, issues, branches, workstreams, specialist roles, legal theories, or capability donors.

Current direction:

```text
DISCOVER
  → RECONSTRUCT_PURPOSE
  → COMPARE_LINEAGE
  → EXTRACT_UNIQUE_VALUE
  → RESTORE_LOST_CAPABILITY
  → COMPOSE_GAINS
  → IMPLEMENT
  → VERIFY
  → INTEGRATE
  → DEPLOY_OR_PACKAGE
  → RECEIPT
```

Inventory, naming similarity, stale dates, proof gaps, or an assistant-generated classification cannot establish redundancy or disposability. Retirement is a separate OPERATOR-authorized decision after verified preservation and a demonstrated stronger result.

The full preserved replacement, including proof semantics, productization, recovery, cross-estate composition, and definition of done, is in the capability-elevation program above. Git history preserves the exact prior document for forensic reconstruction.
